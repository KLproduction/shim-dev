import React from "react";
import ScrollingSection from "./_components/ScrollingSection";

type Props = {};

const page = (props: Props) => {
  return (
    <div className="mx-auto max-w-6xl px-4">
      <ScrollingSection />
    </div>
  );
};

export default page;
